#!/usr/bin/env bash
set -euo pipefail

cd "$(dirname "$0")/.."

mkdir -p clone/wordpress

echo "Starting CLONE environment (project: clone)…"
docker compose -p clone -f docker-compose.yml -f docker-compose.clone.yml up -d

echo "Place your Duplicator files into: clone/wordpress/"
echo "Then open: http://localhost:${WP_PORT:-8080}/installer.php"
